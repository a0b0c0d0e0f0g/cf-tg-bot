name = "cf-tg-bot"
main = "src/index.js"
compatibility_date = "2024-01-01"



[[d1_databases]]
binding = "DB" # 必须是 DB，代码中引用的是这个名字
database_name = "tg-bot"
database_id = "95d33c2e-1568-4f1a-9d9a-95365e3f2aa9"


[[kv_namespaces]]
binding = "LIMIT_KV" # 代码中使用的变量名，必须为 LIMIT_KV
id = "556363f6ea01455682858741bade71e5" # 你创建的 KV 命名空间 ID



# 2. 生产环境配置
[vars]
# 默认管理员账号

# 会话密钥（建议是一串很长的随机字符串，用于加密 Cookie）
SESSION_SECRET = "super-secret-random-string-12345喵喵喵🏳️‍🌈🏳️‍⚧️"

# 注意：ADMIN_PASS（密码）属于敏感信息
# 强烈建议不要写在配置文件里，而是通过 Cloudflare 控制台的 Settings -> Variables -> Secrets 添加
# 或者使用命令：wrangler secret put ADMIN_PASS